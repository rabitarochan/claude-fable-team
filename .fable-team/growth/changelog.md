# このプロジェクトの成長記録(changelog)

> このプロジェクトで /fable-team:grow・/fable-team:retro が行った改善 —— `pj-*` スキルの収穫、
> プロジェクト規約の追加、シグナルの破棄 —— を、根拠と効果測定つきで記録する。
> Fable Team フレームワーク自体への変更は、フレームワークのリポジトリの `CHANGELOG.md` へ。
> 変更が効いたかは次の /fable-team:retro が評価し、効いていなければ /fable-team:grow で書き直すか削る。

---

## 2026-07-08 — grow サイクル 2(シグナル 5 件 → 候補ベースで適用 3・破棄 3・保留 1、効果検証 1)

- **適用**: フレームワーク 3 件(fan-out 校正 / 定時実行の受け口 / retro の /usage 観測)—
  詳細と根拠は CHANGELOG.md の 2026-07-08「Growth cycle 2」エントリに記録
- **効果検証(close the loop)**: growth cycle 1 産の cross-sweep レシピ(verify playbook
  「ドキュメント・ルール類の変更」)は 2026-07-05 の初適用で実欠陥 2 件を検出 = **有効**。
  同シグナルの観察「並列独立ゲート(verifier cross-sweep + reviewer)は検出重複ゼロで相補的」
  も記録 — 直近の 2 変更セットでは reviewer 単独に cross-sweep 観点を同梱する縮退形でも機能
  (must-fix 級の見落としを 2 回捕捉)。二本立てゲートのパターン化は再現待ち
- **破棄(再起票条件つき)**: /goal 停止ゲート(無人走行で早すぎる完了が出たら)/
  handoff-review 見送り 4 分野(実ミッションから該当シグナルが出たら)/ 多視点検証
  (同一反証者の冗長性が実際に失敗したら)
- **保留**: builder 継続実験 — 次の実ミッションで計測してから判断(シグナル自身の規定どおり)
- **Rule diet**: 形骸化ルールなし。追加のみ約 5 行

## 2026-07-08 — 実プロジェクト識別子の流出インシデントと恒久ルール化

- **記録**: 実プロジェクト 2 件の実践レビューから蒸留した 2 変更セット(v0.3.0)で、実プロジェクト名を
  CHANGELOG・growth inbox・コミットメッセージに書いたまま公開リポジトリへ push(**修正シグナル・
  ユーザーが固く禁止を明言**)。対処 = 全箇所を匿名化(project A/B)し、流出コミット 3 件を履歴
  書き換えで削除・force push・タグ付け直し。GitHub 側の残存リスク(SHA 直指定での参照・キャッシュ)は
  ユーザーへ報告し、必要なら GitHub Support でのパージを案内
- **恒久化**: HQ CLAUDE.md「Rules Specific to This Repository」に禁止ルール 1 行を追加
  (Boundary Hygiene の外向き規律をプロジェクトのアイデンティティに拡張)。Conductor の
  auto-memory にも保存(リポジトリ外でも想起できるように)
- **効果測定**: 次に実プロジェクト由来の蒸留を行う変更セットで、レビューゲートの攻撃観点に
  「実プロジェクト識別子の混入」が含まれ、匿名化されて出てくるか

## 2026-07-03 — i18n-publish retro:初の実ミッションでフレームワークを実証

- **記録**: 発足後初の実ミッション `i18n-publish` がエンドツーエンドで成功。過去変更の効果検証 ——
  ①ミッションプロトコル(4 状態ファイル)は**別セッションからの resume を state.md のみで成立**させ実証
  ②成長ループは作業中に 2 シグナル捕獲で稼働 ③検証ハーネス明示化は 4.1 で verifier が再発見コストゼロで検証
  ④委譲ブリーフは全 subagent が結論返答・ダンプなし。**壊れた/不発の変更はなし。** 無人ループ規律のみ未行使
- **署名的パターン(再利用候補)**: 逆翻訳レビュー —— glossary でアフォリズムを定訳固定 → 並列翻訳 →
  英→日に訳し戻して原文突合。英語を読まない発注者が英語成果物を検収できた唯一の方法。並列翻訳で用語割れゼロ
- **根拠**: 本ミッションの journal(resume 成功・4.1 オールグリーン・逆翻訳レビューが設計欠陥 2 件を露出)
- **効果測定**: 次ミッションでも resume が files-only で成立するか、inbox 捕獲が継続するかを次 retro で見る

## 2026-07-03 — 公開手順の環境知見:初回 push は HTTPS remote で

- **記録**: `gh repo create --public --source=. --push` は origin を SSH(git@github.com)で張るため、
  SSH 署名不可の環境では初回 push が失敗する。対処 = `git remote set-url origin https://github.com/<owner>/<repo>.git`
  → `gh auth setup-git` → `git push -u origin main`
- **根拠**: i18n-publish タスク 4.2 で実際に失敗し、上記手順で解決(journal 2026-07-03 GitHub 公開実行の項)
- **効果測定**: 次回の公開/クローン作業でこの手順により初回 push が一発で通るか

<!-- 2026-07-03 retro 由来のフレームワーク変更提案 — 承認結果(発注者判断):
  (A) HANDOFF.md/ja 落とし穴カタログに「翻訳=設計レビュー」を追加 → **承認・適用**(CHANGELOG.md に記録)
  (B) resume スキル原則に「セッション固有前提の明記」を追加 → **却下**(inbox signal を破棄処理)
  (C) mission.md DoD「skills 全 28」→ 検証で 28 は正確(skills 配下ファイル数)と判明。数字は維持し
      単位「配下 28 ファイル(12 スキル)」を補って誤読を解消 → **適用** -->

## 2026-07-04 — brainstorm-asset retro: 無人ループ規律を初実証、収束ルールを1行強化

- **記録**: 2度目の実ミッション brainstorm-asset がエンドツーエンド成功(13タスク+最終検証 ALL MET、1セッション完結)。過去変更の効果検証: ①ミッションプロトコル = フェーズ境界停止・中間報告が機能(resume は今回未行使) ②成長ループ = 5シグナル捕捉で稼働継続 ③検証ハーネス明示化 = 全ブリーフに同梱され機能 ④**無人ループ規律 = 今回初行使で実証**(境界停止・チェックポイント+コミット対・PushNotification)⑤英語化 = 維持、ただし README.ja ペア同期の盲点を露出(→ 別 task で B1+B4 として処理中)。壊れた/不発の変更なし。HTTPS remote 知見・逆翻訳パターンは未行使につき保持
- **署名的パターン(再利用候補)**: AskUserQuestion(multi-select)による収束 — 「複数レンズの選択肢からユーザーが選ぶ」を1回のバッチで完了させる。brainstorm スキルの「the user picks」の実装デバイスとして有効(シグナル3を処理)
- **蒸留結果**: シグナル4(A2撤回・修正)+ シグナル5(反対意見の非明示・摩擦)→ brainstorm SKILL.md 収束ステップに「記録済み反対意見の提示と覆す理由の記録」を1行追加(ユーザー承認済み、CHANGELOG.md に記録)。シグナル1・2 → 別 task(A1+B1+B4+A3)で処理中、完了時に [x] 化。HANDOFF 落とし穴カタログへの追加は冗長と判断し見送り
- **効果測定**: 次 retro で (1) ブレスト収束時の記録済み意見との矛盾が明示されているか (2) resume の files-only 成立(未行使分の持ち越し) (3) multi-select 収束パターンの再現性

## 2026-07-05 — grow サイクル 1(Fable 5 実演): HQ レビュー規約の追加とシグナル 4 件の処理

- **記録**: 初の閾値超過(未処理 6 件)による /fable-team:grow を Fable 5 自身が実演。処理内訳 ——
  ①**HQ 規約追加**: 「フレームワーク変更の敵対レビューは CHANGELOG エントリ草案込みの完成形の
  変更セットに対して実施する」を CLAUDE.md のリポジトリ固有規則に追加(根拠: sweep 4 での
  「エントリ欠落」must-fix ノイズ + sweep 5 で完成形レビューを適用し再発ゼロ = 対策の実証 1 回)
  ②**破棄**: メモリ衛生シグナル — ハーネスのシステムプロンプトが同規律(保存前の重複確認・
  誤記憶の削除・想起事実の使用前再検証)を全 Conductor セッションに配布済みであり、チーム資産への
  転記はプラットフォーム層との重複(Fable 5 の知識による判定。Bloat is death)
  ③**フレームワーク変更 2 件**(verify レシピ・judge 鮮度規律)は CHANGELOG.md 側に記録
  ④**退避継続 2 件**(調査規模校正+多視点レンズ / デプロイ・セキュリティ・性能・API 設計) —
  発火場面が来れば大きな音でシグナルが出る種別(検出バイアスなし)のため inbox に残置。
  処理後の未処理は 2 件 = 閾値内で inbox 汚染も解消
- **署名的パターンの再現**: AskUserQuestion(multi-select)による収束が 2 文脈目(grow の
  変更セット承認ゲート)でも機能 — 4 項目の個別採否を 1 回のバッチで完了。brainstorm 文脈での
  再現を待って SKILL.md への昇格を判断(既定の予定を維持)
- **効果測定**: 次 retro で (1) フレームワーク変更レビューに「エントリ欠落」must-fix が出ない
  (2) memory 起因の混乱シグナルが出たら破棄判定を再訪(根拠はこの記録) (3) multi-select が
  brainstorm 文脈で再現したら昇格を提案

